# Google Maps Scraper - Variabili d'ambiente
# Copia questo file in .env e configura i valori necessari

# ===========================================
# NOTIFICHE TELEGRAM (OPZIONALE)
# ===========================================
# Per configurare le notifiche Telegram:
# 1. Crea un bot con @BotFather su Telegram
# 2. Ottieni il token del bot
# 3. Ottieni il tuo Chat ID
# 4. Configura questi valori in Render

TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# ===========================================
# CONFIGURAZIONE GOOGLE SHEETS
# ===========================================
# Per Google Sheets, usa una di queste opzioni:

# Opzione 1: Service Account (Raccomandato)
# Crea un Service Account in Google Cloud Console
# Scarica il file JSON e copia il contenuto qui:
GOOGLE_CREDENTIALS={"type": "service_account", "project_id": "..."}

# Opzione 2: OAuth (Più semplice)
# Usa le tue credenziali Google personali
GOOGLE_ACCESS_TOKEN=your_access_token_here

# ===========================================
# CONFIGURAZIONI AVANZATE (OPZIONALE)
# ===========================================
# Timeout per le richieste (millisecondi)
CONTACT_TIMEOUT=12000

# Numero massimo di worker paralleli
# NUM_WORKERS=4

# Delay tra le richieste (secondi)
# DELAY_MIN=2
# DELAY_MAX=5

# Tempo massimo di sessione (secondi) - 2 ore di default
# MAX_SESSION_TIME=7200